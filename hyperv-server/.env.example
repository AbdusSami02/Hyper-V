PORT=4000
CORS_ORIGIN=http://localhost:5173

# Connection string for your MongoDB database. For local development,
# either run MongoDB locally (mongodb://localhost:27017/hyperv) or just
# use a free MongoDB Atlas cluster — see the README for setup.
MONGODB_URI=mongodb+srv://abdussamijawed1_db_user:zD2zuQ0fKiMWwP4C@hyperv.stcwdoy.mongodb.net/?appName=hyperV

# Password for the /admin page in the frontend. Set this to something real —
# there's no default, and the admin login endpoint will refuse to work
# without it.
ADMIN_PASSWORD=Abdussami@12
