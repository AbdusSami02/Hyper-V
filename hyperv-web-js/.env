# Only needed for production builds where the backend isn't reachable via
# the dev proxy. Leave unset in local dev — vite.config.js already proxies
# /api requests to http://localhost:4000.
# VITE_API_URL=https://api.your-domain.com
